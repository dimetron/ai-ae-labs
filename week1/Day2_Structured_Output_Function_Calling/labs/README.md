# Домашнє завдання 2 — інструкція до лабораторної: Strict Schema Enforcer

**Тиждень 1 · День 2** · Мова: Go · `google.golang.org/adk/v2 v2.2.0`, Go 1.27
(ADK вимагає ≥1.26.5) — **станом на 08/2026**.

Лекція: [`courses/AI_Agents_Engineering/lectures/week1/Day2_Structured_Output_Function_Calling/Lecture.md`](../Lecture.md) ·
ДЗ: [`Homework.md`](../Homework.md)

---

## Ранній win — 3 хвилини, **без API-ключа й без мережі**

```bash
cd courses/AI_Agents_Engineering/lectures
go run ./week1/Day2_Structured_Output_Function_Calling/labs -offline console
```

**Очікуваний вивід:** у stderr — рядок `rates: offline fixture (USD/EUR/PLN)`,
далі консольний launcher. Спитайте: `Скільки буде 100 доларів у гривнях?`

Це навмисно перший крок дня: **ви маєте побачити типізований виклик
інструмента раніше, ніж витратите час на ключі**. Прапорець `-offline` бере
фікстурні курси замість живого API НБУ — жодних зовнішніх залежностей.

Далі — з живими курсами й реальною моделлю:

```bash
export GOOGLE_API_KEY=...
go run ./week1/Day2_Structured_Output_Function_Calling/labs console
```

---

## Що саме тут відбувається

Сьогоднішня межа однією фразою: **модель не виконує інструмент — вона просить
його викликати, а виконує довірений код**. У цій лабі цей поділ видно фізично:

| Шар | Файл | Хто йому довіряє |
|---|---|---|
| Опис інструмента (`name`, `description`, схема параметрів) | `agent.go` — `functiontool.New` | Модель читає це й вирішує, чи кликати. |
| Типізований вхід/вихід | `rates.go` — `RateInput` / `RateOutput` | Компілятор. Схема виводиться з Go-структур — **один source of truth**. |
| Валідація на межі | `rates.go` — `normalizeCode`, `Convert` | Ваш код. Тут і тільки тут вирішується, чи запит взагалі законний. |
| Виконання дії | `rates.go` — `NBUProvider.RatesToUAH` | Ваш код. Модель сюди не дістає. |

Ключове — два різні рівні відмови, і обидва спрацьовують **вже після** того, як
JSON від моделі виявився бездоганно валідним:

- `normalizeCode` — форма (`ErrInvalidCode`): три літери, ISO 4217.
- `rateToUAH` — реальність (`ErrUnknownCurrency`): такий код існує, але його
  немає в довіднику НБУ.

Друга перевірка і є `shape ≠ intent` у чистому вигляді: схема гарантувала
*форму*, а не *намір*, не *існування* й не *дозвіл*. Жодна JSON Schema не
знає, які валюти сьогодні котирує НБУ — це знає ваш код, і саме тому валідація
живе на межі, а не в описі інструмента.

### Вибір провайдера

Перший збіг виграє, порядок задокументований навмисно (`agent.go`, `BuildModel`):

| Змінна | Бекенд | Навіщо |
|---|---|---|
| `AGENTGATEWAY_BASE_URL` | `openaimodel` через локальний шлюз | Найвищий пріоритет: коли гейтвей піднято, **весь** трафік має йти крізь нього, інакше в трасах і в обліку вартості будуть дірки. |
| `OLLAMA_BASE_URL` | `openaimodel` з власним `BaseURL` | Self-hosted. Апстрім-провайдера Ollama в ADK немає — це підтримуваний шлях. |
| `GOOGLE_API_KEY` | `gemini` | Дефолт курсу. |
| `OPENAI_API_KEY` | `openaimodel` | — |

> ⚠️ **`ANTHROPIC_API_KEY` сам по собі не запустить лабу.** ADK Go v2.2.0
> постачає рівно три модельні пакети — `gemini`, `openaimodel`, `apigee`.
> Бекенда Anthropic **немає** (звірено в module cache 27.08.2026). Це реальне
> обмеження фреймворка, а не недогляд вправи — і код каже це прямо, замість
> падати незрозумілим 401. Це ж чесна «ціна вибору» ADK Go з порівняльної
> таблиці Дня 1.

---

### Дві теки з кодом — не переплутайте

У курсі є **два окремі Go-модулі**, і це навмисно:

| Тека | Що це | Для кого |
|---|---|---|
| `labs/` (у цій теці) ← **ви тут** | ваш робочий модуль: стартери й розв'язки ДЗ, корениться у `courses/AI_Agents_Engineering/lectures/` | студент |
| `courses/AI_Agents_Engineering/code/week1/part2/` | instructor demo: той самий контракт `open_refund_case` уже як `workflow.NewFunctionNode[RefundCaseRequest, RefundCaseResponse]` | лектор на екрані |

Демо показують у Блоці 4 як «++ Advanced». Дивитись його корисно, **запускати
для здачі — не потрібно**: це інший модуль зі своїм `go.mod`.

> ⚠️ **Відомий борг (не ваша проблема, але знайте):** ці два модулі пінять
> **різні мінорні версії ADK** — лаби на `v2.2.0`, демо на `v2.0.0`
> (станом на 27.08.2026). На матеріал Тижня 1 це не впливає, обидва зелені;
> питання узгодження пінів охоплює тижні 1–6 і вирішується окремо.

## Core path — що має бути в здачі

1. Доменна структура як Go-struct із тегами.
2. JSON Schema, **виведена** зі структури, а не написана руками другий раз.
3. Виклик моделі зі structured output.
4. Десеріалізація у структуру + валідація на межі.
5. **Fail path**: невалідний вихід або ремонтується, або чесно падає.
   Регулярка по тексту — не відповідь.
6. У README — коротке пояснення «Prompt vs Contract» своїми словами.

> **Правило «своїми словами» — не формальність.** У RCT PNAS (Bastani et al.,
> 2025, ~1 000 студентів) група з AI-помічником була на +48 % кращою на
> практиці й на −17 % гіршою на іспиті без нього. Це не «не користуйтесь
> інструментом» — це «швидше зробиш ≠ швидше навчишся». Пункт 6 не оцінюється
> строго й не виноситься в тест; він тут заради вас.

---

## Тести

```bash
cd courses/AI_Agents_Engineering/lectures
go test ./week1/...
```

**Очікуваний вивід:** усі пакети `ok`. Ключа й мережі не потрібно —
`FixtureProvider` і `httptest` закривають обидва шляхи.

> **Відома пастка середовища:** `TestNBUProvider` піднімає `httptest.NewServer`,
> тобто слухає на `127.0.0.1`. У пісочниці без права `listen` він падає з
> panic у `newLocalListener` — це **обмеження середовища, а не червоний тест**.
> Перевірено 2026-08-27: у звичайному терміналі всі три пакети зелені.

---

## Якщо щось не працює

| Симптом | Причина | Що робити |
|---|---|---|
| `no model provider configured` | жодної зі змінних не видно | Почніть з `-offline`: він не потребує ключа взагалі. |
| `ANTHROPIC_API_KEY is set, but ADK Go v2.2.0 ships no Anthropic backend` | ключ є, бекенда немає | Це не баг. Дайте `GOOGLE_API_KEY` або підніміть Ollama. |
| Модель повертає JSON у ```-блоці | провайдер обгортає відповідь | Не «чистіть» регуляркою — це і є урок Блоку 2. Вмикайте structured output на рівні API. |
| `invalid currency code: "долар" must be three letters (ISO 4217)` | не пройшла валідація форми | **Так і має бути.** JSON від моделі валідний, але значення поля — ні. |
| `unknown currency code: XYZ` на бездоганному JSON | код валідний за формою, але його немає в довіднику | **Це і є `shape ≠ intent`**: форма правильна, а намір не підкріплений реальністю. Схема цього не ловить — ловить `rateToUAH`. |
| Живий НБУ не відповідає | зовнішній сервіс | `-offline` — фікстури; лаба від доступності НБУ не залежить. |
| Модель вигадала виклик неіснуючого інструмента | phantom tool call | Валідуйте **ім'я** інструмента, а не лише аргументи. Розбір — Блок 3 лекції. |

---

## 🔥 Бонус-трек (не оцінюється, сертифікат не гейтить)

Цілі, не кроки.

1. **Fault injection.** Змусьте провайдера повертати три класи збоїв із
   таксономії *AgentChaos* (arXiv:2608.06790) — omission, value, schema — і
   покажіть, що кожен веде до **явного** результату: ремонт або чесна помилка,
   ніколи до тихої правдоподібної відповіді.
2. **Вузол графа.** Загорніть валідацію у
   `workflow.NewFunctionNode[RateInput, RateOutput]` і налаштуйте retry явно
   через `NodeConfig.RetryConfig`. Це прямий місток у Тиждень 2.
3. **Ціна поверхні інструментів.** Додайте до агента 20 фіктивних інструментів
   і виміряйте, як змінюється точність вибору потрібного. Контрольований
   вимір (arXiv:2608.14036) дає деградацію 29,6 % → 3,3 % при рості пулу
   5 → 100 — перевірте порядок величини на своєму випадку.
4. **Бюджет виходу.** Обмежте розмір відповіді інструмента й покажіть у
   README, скільки токенів це економить на 10 кроках агентного циклу.
